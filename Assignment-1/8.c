// 8. Write a program to calculate square of two numbers. Numbers are taken from user through keyboard.

#include<stdio.h>
int main()
{
    int a;
    printf("Enter any numbers: ");
    scanf("%d",&a);
    printf("Square is %d",a*a);
    return 0;
}